<% if current_user.likes.where(photo_id: params[:photo]).present? %>
  $("input.like_button").css("background", "grey").text("Liked!")
<% end %>

$(".like_number").text("<%= Photo.find(params[:photo]).likes.count %>")