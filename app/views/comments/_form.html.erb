<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :comment, "Reply to thread" %><br>
    <%= f.text_area :comment %>
  </div> 
    <%= f.submit %>
<% end %><br>
