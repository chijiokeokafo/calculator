<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @classified.title %>
</p>
<strong>Description:</strong> <br><%= @classified.description %><br>
<strong>Price:</strong><br> $<%= @classified.amount %>.00 <br>
I'm located in <strong><%= @classified.city %></strong><br>

<% @classified_attachments.each do |p| %>
  <%= image_tag p.picture_url, :size => "150x150" %>
  <%# link_to "Destroy", p, method: :delete %>
<% end %>

<%= link_to 'Edit', edit_classified_path(@classified) %> |
<%= link_to 'Back', classifieds_path %>




<% if current_user %>
  <h4>Leave your reply to my post here</h4>

  <%= form_for([@classified, @poster]) do |f| %>
    <% if @poster.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@poster.errors.count, "error") %> prohibited this poster from being saved:</h2>
        <ul>
        <% @poster.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div class="field">
      <%= f.label :email %><br />
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :message %><br />
      <%= f.text_area :message %>
    </div>
    <div class="actions">
      <%= f.submit "Send Email" %>
    </div>
  <% end %>

  <% end %>
