<h1> Recent Photos </h1>
<div id="home-specs">
	<% Photo.recent(10).each do |photo|%>
	  <div id="photorecent">
		  <%=link_to image_tag(photo.avatar_url(:large)), "/photos/#{photo.id}" %>
	  </div>
	<% end %>
</div>			